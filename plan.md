# Java Agent 学习计划（课时制）

> **按课时推进，不绑日期。**
> 1 课时 ≈ 1 小时。时间多的日子可以连做 3-5 个课时，时间少的日子做 1 个。
>
> **每个课时都有独立可验证的产出** —— 中断了也不丢上下文，下次接着往下做。

---

## 使用方式

| 规则 | 说明 |
|---|---|
| **课时是刻度，不是任务** | 某天有 4 小时 → 连做 4 个课时；1 小时 → 做 1 个 |
| **每个课时都能独立验证** | 末尾有"验收"项，做完能确认效果 |
| **卡住超过 40 分钟就跳过** | 记进 `agent-notes.md` 的坑清单，继续往下。别在一个点上耗尽体力 |
| **长任务不拆分** | 标注 ⚠️ 的课时不适合碎片时间做（需连续专注） |

---

## 进度总览

| 阶段 | 课时数 | 累计课时 | 状态 |
|---|---|---|---|
| 预备周 | 5 | 5 | ✅ **完成** |
| **W1** LLM 客户端 | 6 | 11 | 🚧 进行中 |
| **W2** 手写 Tool Calling 循环 ★ | 7 | 18 | ⬜ |
| **W3** Spring AI + ReAct | 6 | 24 | ⬜ |
| **W4** 记忆 + RAG | 7 | 31 | ⬜ |
| **W5** Plan-and-Execute + LangGraph | 5 | 36 | ⬜ |
| **W6** 工程化 I | 5 | 41 | ⬜ |
| **W7** 工程化 II | 5 | 46 | ⬜ |
| **W8** 文档 + 简历 | 4 | 50 | ⬜ |

**总计约 50 课时。** 按每周 8-10 课时算，约 5-6 周（比原 8 周紧，可按实际情况放缓）。

---

## ✅ 预备周（课时 1-5）—— 已完成

| # | 内容 | 产出 |
|---|---|---|
| 1 | 环境搭建（JDK/WSL/Docker/PgVector/Redis） | 容器跑起来 |
| 2 | API 打通 | curl 调通 DeepSeek |
| 3 | ReAct 综述 | 理解 Agent 循环 |
| 4 | Plan-and-Execute 综述 | 选型判据 |
| 5 | Function Calling 规范 + SpringBoot 骨架 | 项目能启动 + 配置验证 |

**验收**：✅ 已通过（Agent 循环机制、轮次 vs 工具调用、选型判据、maxRound 定位）

---

## 🚧 W1：基础可靠的 LLM 调用服务（课时 6-11）

**核心目标**：**不要 Agent，不要工具**。先写一个稳定、可运维的 LLM HTTP 客户端。

> 路线图原文：**这周绝对不要碰 Tool Calling、不要碰 RAG。**

---

### 课时 6｜定义接口 + HTTP 客户端骨架（1.5h）

**做什么**
- 定义 `LlmClient` 接口：`chat(request)` → `ChatResponse`
- 用 `RestClient`（Spring 6.1+ 内置，比 `RestTemplate` 现代）或 `WebClient` 实现
- 请求/响应 DTO（先只做必要的字段）
- 从配置读 `api-key` / `base-url` / `model`

**验收**
- [ ] 一个单元测试或临时 `CommandLineRunner`，能打印出模型回复
- [ ] 日志里能看到发出去的 URL 和返回的 model

**注意**：这一步**不做**超时、重试、流式 —— 先把基本链路跑通。

---

### 课时 7｜`POST /chat` 非流式接口（1.5h）

**做什么**
- `ChatController`：`POST /api/chat`
- 请求体：`{ "sessionId": "...", "message": "..." }`
- 响应体：`{ "reply": "...", "usage": {...} }`
- 错误处理：LLM 调用失败时返回结构化错误

**验收**
- [ ] curl 或浏览器能调通，看到回复
- [ ] 日志里有 `usage` 的 token 数（用 §3 的日志字段清单）

---

### 课时 8｜SSE 流式输出 —— 服务端（2.5h）⚠️ 需连续专注

**做什么**
- `GET /api/chat/stream/{sessionId}` 返回 `text/event-stream`
- 用 `WebClient` 读 DeepSeek 的 SSE 流
- **解析每一行**：去掉 `data: ` 前缀 → 提取 `delta.content` → 转发
- 处理三种情况：普通 chunk / **空 delta（最后一条）** / `[DONE]`

**验收**
- [ ] `curl -N http://localhost:8080/api/chat/stream/xxx` 能看到**逐字输出**
- [ ] `[DONE]` 后连接正常关闭，不报错

**⚠️ 这是 W1 最难的一课**，坑在这三处：
1. `delta.content` 可能是 `null`（最后一条 chunk）→ **必须判空**
2. 一个 chunk 可能包含**多行** `data:` → 按行切分
3. 转发时要**保持 SSE 格式**（`data: xxx\n\n`）

---

### 课时 9｜流式的收尾与容错（1.5h）

**做什么**
- 客户端断开连接时，**服务端要能感知并停止**（否则白烧 token）
- 流式过程中的异常 → 以 SSE 形式推给客户端（`event: error`）
- 流结束后**把完整回复落库/落 Redis**
- 补一个「非流式」和「流式」共用底层逻辑的重构

**验收**
- [ ] 中途 `Ctrl+C` 断开，服务端日志显示检测到断开
- [ ] 流结束后能查到完整回复

---

### 课时 10｜超时 + 指数退避重试（2h）

**做什么**
- 连接超时 10s、读超时 60s（流式单独配）
- **指数退避重试**：1s → 2s → 4s，最多 3 次
- **只对可重试错误重试**：
  - ✅ 网络异常、连接超时、`429` 限流、`5xx`
  - ❌ `400` / `401` / `402`（参数错、认证错、余额不足）—— **重试也没用，白烧配额**
- 重试时记日志：第几次重试、什么原因

**验收**
- [ ] 单元测试：mock 一个 `429`，验证重试了 3 次
- [ ] 单元测试：mock 一个 `401`，验证**没有**重试

**⭐ 面试高频点**：为什么业务错误不能重试？

---

### 课时 11｜traceId + MDC 全链路日志（1.5h）

**做什么**
- 每个请求生成 `traceId`（或从请求头透传），放进 `MDC`
- 在 Agent 运行的**每个环节**都带同一个 traceId：
  - HTTP 请求进入 → LLM 调用 → 工具调用 → 返回
- 日志格式加上 `%X{traceId}`（`application.yml` 里已配好）
- **按 §3 的字段清单记录**：请求 model / 实际 model / fingerprint / tokens / cache 命中 / finish_reason / 耗时

**验收**
- [ ] 用同一个 traceId 能在日志里搜到**完整的一次调用链**
- [ ] **确认日志里没有明文 api-key**

---

### 课时 11.5｜会话历史存 Redis（1.5h）

**做什么**
- `user id + sessionId` 作为 key，消息历史存 Redis
- 设置过期时间（比如 30 分钟）
- **重启服务后会话不丢**
- 消息格式要和 `messages` 数组结构一致（为 W2 铺路）

**验收**
- [ ] 聊几轮 → 重启应用 → 继续聊，模型记得之前的内容
- [ ] Redis 里能看到存的 key（`redis-cli KEYS "*"`）

---

### W1 完成后你应该能回答

- [ ] SSE 和 WebSocket 的区别？Agent 服务为什么优先选 SSE？
- [ ] 请求 ID 链路追踪怎么做？为什么 Agent 服务特别需要？
- [ ] 什么情况下 LLM 调用可以重试，什么情况不能？
- [ ] 流式输出里 `delta` 和 `message` 有什么区别？

---

## ⬜ W2：手写 Tool Calling 循环 ★ 全路线最重要（课时 12-18）

> **整条路线的重中之重，也是面试最大的杀招。**
> 没有 Spring AI、没有 LangGraph，**纯 Java 业务代码自己驱动完整循环**。

---

### 课时 12｜`Tool` 抽象 + 两个本地工具（2h）

**做什么**
- `Tool` 接口：`name` / `description` / `jsonSchema` / `execute()`
- `ToolRegistry`：注册和查找
- 实现 `get_current_time` 和 `calculator`
- **每个工具自己提供 JSON Schema**（参考 `agent-notes.md` §8）

**验收**
- [ ] 能通过 registry 名字拿到工具并执行
- [ ] 打印出生成的 `tools` 数组 JSON，格式能通过 API 校验

---

### 课时 13｜循环骨架（2.5h）⚠️ 需连续专注

**做什么**
- 改造 `/chat`：带上 `tools` 调用 LLM
- **手写 for 循环**：解析 `finish_reason` → 分支处理
- 状态累积：`messages` 里追加 `assistant`（含 tool_calls）和 `tool` 消息
- **`maxRound = 5` 强制终止**

**验收**
- [ ] 问"现在几点"→ 日志显示调用了 `get_current_time` → 返回正确时间
- [ ] 问"3+5 等于多少"→ 调用了 `calculator`
- [ ] 问"你好"→ **没有**调用工具，直接回答

**⭐ 核心**：终止条件、循环控制**全部是你的 Java 代码**，不是框架。

---

### 课时 14｜并行工具调用（1.5h）

**做什么**
- ⚠️ `tool_calls` 是**数组** → `for` 遍历执行
- 每个结果带**对应的 `tool_call_id`** 塞回 messages
- 处理工具执行失败的单个结果（不影响其他工具）

**验收**
- [ ] 问"北京和上海现在几点"→ **一次响应返回 2 个 tool_calls** → 都执行 → 一起回传

---

### 课时 15｜参数校验 + 异常回灌（2h）

**做什么**（参考 `agent-notes.md` §7 Q10）
- 五道校验：工具名存在 / JSON 合法 / 必填齐全 / 类型正确 / 值域合法
- **校验失败 → 错误信息塞回 messages**（不要抛异常）
- 参数解析失败同样回灌，让模型自我纠错

**验收**
- [ ] 故意让模型传错参数（比如 `calculator` 传 `"abc"`），日志显示：错误回灌 → 模型重试 → 成功

**⭐ 这是 Agent 的自我纠错机制**，面试很值钱。

---

### 课时 16｜循环防护 + 可观测性（1.5h）

**做什么**
- **检测重复调用**：相同工具 + 相同参数已调过 → 提前终止（比等 maxRound 更早）
- **空响应处理**：`content` 和 `toolCalls` 都空 → 有限重试 → 兜底话术
- 每轮详细日志：`第 3 轮，调用 calculator，参数 2+3，返回 5`
- 达到 maxRound 时的**降级返回**（告知已完成部分，不假装成功）

**验收**
- [ ] 日志能完整还原一次工具循环的每一轮
- [ ] 人为构造重复调用，验证提前中断

---

### 课时 17｜联网/复杂工具演练（1.5h）

**做什么**
- 加第 3 个工具：调用一个公开免费 API（如天气/汇率/公开搜索）
- 验证多工具下的**选择准确性**（参考 `agent-notes.md` §9）

**验收**
- [ ] 三个工具都能被正确选中
- [ ] 记录一次"模型选错工具"的案例（如果发生），分析原因

---

### 课时 18｜W2 总结 + 面试题自测（1h）

**做什么**
- 整理代码注释，理清循环的每一层
- **不看笔记**回答 W2 面试题（`qa-notes.md` 🔴 组 17 题）

**面试题**
- [ ] 没有 LangGraph，Tool Calling 完整循环怎么实现？
- [ ] 终止条件怎么控？怎么防无限调用？
- [ ] 工具调用失败了怎么处理？
- [ ] 模型一次返回多个工具调用怎么处理？

---

## ⬜ W3：Spring AI 重构 + 基础 ReAct（课时 19-24）

> 心态：**"我已经手动知道循环怎么跑了，现在 Spring AI 只是帮我省掉写重复循环的体力活"** —— 而不是"我不知道原理，靠框架帮我跑"。

---

### 课时 19｜引入 Spring AI 2.0（1.5h）

**做什么**
- 加 `spring-ai-bom` + starter 依赖
- ⚠️ **注意**：Spring AI 2.0 要求 Spring Boot 4（你正好是 4.1.1，兼容）
- 用 `ChatClient` 重写一次最简单的调用，**先不碰工具**

**验收**
- [ ] Spring AI 能调通 DeepSeek（配置 `openai` 兼容模式，base-url 指向 DeepSeek）
- [ ] 记录与手写客户端的**差异点**（配置方式、返回结构）

---

### 课时 20｜用 Spring AI 迁移工具调用（2.5h）

**做什么**
- 把 W2 的 `Tool` 迁移到 Spring AI 的 `@Tool` 注解 / `ToolCallback`
- **对比**：框架替你做了什么？哪些逻辑它没做？
- 保留手写版本的代码（不要删，面试要用）

**验收**
- [ ] 同样的三个工具，用 Spring AI 能跑通
- [ ] **写一段对比笔记**：手写版 vs 框架版，各自的控制点在哪

---

### 课时 21｜激活 ReAct 的 thought 输出（2h）

**做什么**
- 配置让模型**显式输出思考过程**（参考 `agent-notes.md` §7 Q3）
- **观察**：不配置 vs 配置，输出有什么变化
- 把 thought 通过 SSE 实时推给用户

**验收**
- [ ] 能看到模型的 `Thought` 内容
- [ ] **验证"prompt 诱导"**：去掉诱导 prompt，thought 消失

**⭐ 这一课是理解 ReAct 的关键**：thought 不是模型天生就有的，是 prompt 出来的。

---

### 课时 22｜新增联网搜索工具（2h）

**做什么**
- 接入一个**公开免费搜索 API**（DuckDuckGo / 硅基流动搜索插件）
- ⚠️ **不要自己搭爬虫** —— 会引入大量脏活分散注意力
- 处理搜索结果过大 → **截断/摘要后再塞进 messages**（否则 token 爆炸）

**验收**
- [ ] 问需要实时信息的问题，Agent 会联网搜索并回答
- [ ] 日志显示搜索结果被控制了大小

---

### 课时 23｜工具集整理 + W3 收尾（1.5h）

**做什么**
- 工具总数控制在 **3-4 个**（`agent-notes.md` §9：多了模型选不准）
- 优化每个工具的 `description`（写清"什么时候用"）
- W3 面试题自测

**验收**
- [ ] 所有工具都能被正确触发
- [ ] 能说清"为什么工具不能太多"

---

### 课时 24｜缓冲 / 补课（1.5h）

**留给**：W2/W3 中卡住的部分、没跑通的验收项、想深入的细节。

---

## ⬜ W4：记忆系统 + RAG（课时 25-31）

**两类记忆不要混淆**：
- **短期记忆** = 会话上下文（Redis，W1 已做）
- **长期记忆** = 持久化的用户事实（W4 新做）
- **RAG** = 文档知识库（W4 新做）

> ⚠️ **很多 Demo 把全部聊天历史扔进向量库，是错的。**

---

### 课时 25｜PostgreSQL + PgVector 接入（1.5h）

**做什么**
- 加 PostgreSQL 驱动 + JPA/JDBC 依赖（**现在 pom 里还没有**）
- 建表：`document_chunk`（含 `embedding vector(n)`）
- 验证连接和 vector 类型可用

**验收**
- [ ] `\dt` 能看到表
- [ ] 能插入并查询一条向量（参考 `agent-notes.md` §11.6）

---

### 课时 26｜Embedding API 封装（1.5h）

**做什么**
- 调 DeepSeek 或硅基流动的 Embedding API 拿向量
- 封装成 `EmbeddingClient`
- 注意**批量调用**和**维度**问题

**验收**
- [ ] 输入一段文本，返回向量，维度正确
- [ ] 记录 API 的限流规则

---

### 课时 27｜RAG 链路 A：文档入库（2.5h）⚠️

**做什么**
- 文件上传接口（Markdown / PDF）
- **PDF 解析**：用 Apache PdfBox，简单提取文本即可（**别引入复杂的大模型文档解析**）
- **文本分块**：先实现**固定长度分块**，之后加可选重叠分块
- 调 Embedding → 存入 PgVector

**验收**
- [ ] 上传一个 md 文件，库里能查到分块记录
- [ ] 打印分块结果，检查是否合理

**⚠️ 路线图明确要求**：先跑通基础链路，**不要一开始就上复杂语义分块**。

---

### 课时 28｜RAG 链路 B：检索 + 注入（2h）

**做什么**
- 用户提问 → 生成 query embedding → PgVector 检索 top-4
- **把检索结果注入 System Prompt**
- LLM 基于文档内容回答
- 加相似度阈值（太低的不注入）

**验收**
- [ ] 问文档里的内容能准确回答
- [ ] 问文档外的问题，不会强行编造
- [ ] 日志能看到检索到的片段和相似度

---

### 课时 29｜长期记忆：抽取 + 存储（2h）

**做什么**
- 每完成 **4-5 轮对话**，主动发 prompt 抽取记忆：
  > "从历史对话提取重要事实（用户偏好、个人信息、事实），排除临时问答。以 JSON 列表返回。"
- 记忆 → Embedding → 存 PgVector
- 新会话开始前检索 **top-3** 相关记忆，注入 System Prompt

**验收**
- [ ] 告诉 Agent "我是 Java 后端，6 年经验" → 新会话它能记得
- [ ] 库里有记忆记录

---

### 课时 30｜混合检索（可选，2h）

**做什么**
- Postgres **全文关键词检索** + **向量检索** 合并结果（hybrid search）
- 用 `pg_trgm`（已建好扩展）或 `tsvector`

**验收**
- [ ] 对比纯向量检索和混合检索的效果差异

> 有余力才做，**不强求**。

---

### 课时 31｜W4 复盘 + 写设计取舍（1.5h）

**做什么**
- **在 README 的「设计取舍」章节补充**（面试最爱听 trade-off）：
  - 固定重叠分块 → 语义分块会提升质量但增加成本
  - 无重排器（Reranker）→ 未来可加
  - 长期记忆简单抽取 → 缺遗忘和冲突检测
- W4 面试题自测

**验收**
- [ ] README 更新
- [ ] 能说清 RAG 每一步的工程问题和缓解措施

---

## ⬜ W5：Plan-and-Execute + LangGraph（课时 32-36）

**两个目标**：
1. Java 里加计划能力
2. 快速写个 Python LangGraph 原型（**只为理解，不作简历主项目**）

---

### 课时 32｜Plan-and-Execute：规划器（2h）

**做什么**
- 用户目标 → 调 LLM 生成**步骤清单**（JSON 数组）
- 处理计划解析失败
- **计划可展示给用户**（配合 SSE）

**验收**
- [ ] "帮我分析一下 XX" → 生成一份合理的多步计划
- [ ] 计划能被解析成结构化数据

---

### 课时 33｜Plan-and-Execute：执行器（2.5h）⚠️

**做什么**
- **代码遍历计划列表**逐条执行（参考 `agent-notes.md` §10.2）
- 每步内部可调用已有工具 / RAG
- **每步执行后可让 LLM 更新计划**（增删步骤）← 对"计划刚性"的补救
- 步骤间的日志、进度持久化

**验收**
- [ ] 多步任务能完整跑完
- [ ] 中途失败能重新规划，而不是整个推翻

**⭐ 关键认知**：**控制流在代码手里，不在 LLM 手里。**

---

### 课时 34｜LangGraph QuickStart（2h）

**做什么**
- 用 Python 跑通 LangGraph 官方 QuickStart
- 理解四个概念：**Node / Edge / State / 条件边**

**验收**
- [ ] 能跑通官方示例
- [ ] 能说出 "LangGraph = 通用可持久化状态机"

> ⚠️ **不要花时间美化 Python 版本。** 代码放 `python-proto/`，README 注明"原型验证用，非主项目"。

---

### 课时 35｜对比总结（1.5h）

**做什么**
- 写对比笔记：**手写循环 / Plan-Execute / LangGraph 三者关系**
- 回答：为什么主项目用 Java，原型用 Python？

**验收**
- [ ] 能说清"Plan-Execute、ReAct 本质都是特殊的状态流转，LangGraph 是通用状态机"

---

### 课时 36｜缓冲（1h）

---

## ⬜ W6：工程化 I —— 可观测性 / 异常 / 鉴权限流（课时 37-41）

> **全部是后端技能，6 年 Java 经验正好发挥。**
> **不要嫌"这些跟 AI 没关系"** —— AI 服务出问题排查更难，可观测性是生产落地第一刚需。

---

### 课时 37｜统一异常处理（2h）

**做什么**
- 全局异常处理器
- **区分异常类型**：业务异常 / 外部 API 异常（LLM、向量库）/ 参数异常
- 标准化错误码和错误消息

**验收**
- [ ] 各类错误返回统一的 JSON 结构
- [ ] LLM 调用失败时用户能看懂错误原因

---

### 课时 38｜Redis 限流（2h）

**做什么**
- 用 Redis 实现简单 API 限流
- **同一个 sessionId 每分钟最多 10 次请求**
- 保护下游 LLM API 不被打满（LLM API 有 QPS 限制）

**验收**
- [ ] 快速请求 11 次，第 11 次返回 429
- [ ] 限流日志清晰

> 不用复杂网关，**应用内简单限流足够**。

---

### 课时 39｜API Key 鉴权（1.5h）

**做什么**
- 请求头带 `apikey`，内存或 Redis 校验
- **不用 OAuth2 那么重** —— 有身份校验意识即可

**验收**
- [ ] 无 key 或错误 key 返回 401
- [ ] 正确 key 正常访问

---

### 课时 40｜完善全链路日志（2h）

**做什么**
- **每一次 Agent 运行**：每一轮工具调用、每一次 RAG 检索、每一次记忆抽取，**都带同一个 traceId**
- 记录**输入输出摘要**（不要记录完整 prompt）
- 把调试接口（`/api/config-check`）要么删掉、要么放在鉴权后面

**验收**
- [ ] 一次完整的 Agent 运行能在日志里被完整还原
- [ ] 确认无密钥、无超长文本

---

### 课时 41｜W6 收尾（1.5h）

**做什么**
- README 补充：生产环境可对接 **OpenTelemetry**（知道是什么就行，**不用实现**）
- 说明：不需要 ELK，标准日志输出即可

**验收**
- [ ] 项目"像一个正规后端服务"，而不是随手跑的脚本

---

## ⬜ W7：工程化 II —— 持久化 / 长任务 / 容器编排（课时 42-46）

---

### 课时 42｜会话历史落盘 PostgreSQL（2.5h）

**做什么**
- 设计表结构：`session` / `message` / `agent_run`
  - `agent_run`：每一次完整 Agent 运行一条记录（traceId、开始结束时间、最终状态）
- Redis → PG 持久化，**Redis 退化为缓存加速**

**验收**
- [ ] 重启后历史还在
- [ ] 每次 Agent 运行都有记录可查

---

### 课时 43｜长任务方案设计（1.5h）

**做什么**
- **只写方案，不用完整实现**（路线图明确允许）
- 方案：提交任务 → 返回 `taskId` → 后端异步处理（线程池）→ 轮询查询
- **风险点要写清**：并发控制、任务状态持久化
- 写进 README

**验收**
- [ ] README 里有完整的长任务设计章节
- [ ] 能说清"HTTP 接口会超时"这个问题和解法

> ⭐ **这是后端工程师的价值** —— 很多脚本选手根本不思考超时问题。

---

### 课时 44｜Dockerize + docker compose 一键起（2h）

**做什么**
- 写 `Dockerfile`（多阶段构建）
- 完善 `docker-compose.yml`：**app + postgres(pgvector) + redis**
- **任何人 clone 后 `docker compose up` 就能跑**

**验收**
- [ ] 从零 clone → `docker compose up -d` → 服务可用
- [ ] README 快速开始部分是准确的

---

### 课时 45｜OpenAPI（Swagger）文档（1.5h）

**做什么**
- 加 SpringDoc OpenAPI 依赖
- 自动生成接口文档
- 给接口加描述和示例

**验收**
- [ ] 访问 `/swagger-ui.html` 能看到所有接口

---

### 课时 46｜W7 收尾（1h）

**做什么**
- 端到端回归测试
- 整理所有遗留问题

---

## ⬜ W8：文档 + 简历素材（课时 47-50）

> **不要加新代码功能！** 只做文档整理和面试口述梳理。

---

### 课时 47｜README 定稿（2h）

**结构参考**（当前 README 已有雏形，补充完善）：
- [ ] 项目简介（明确：**没有使用低代码**）
- [ ] **Mermaid 架构图**（GitHub 原生支持，不需要图片）
- [ ] 功能清单
- [ ] 快速启动指南（`docker compose up` 一步跑通）
- [ ] **设计取舍（最重要章节）** —— 补全所有 trade-off
- [ ] 已知局限、未来改进方向

---

### 课时 48｜面试口述提纲（2h）

**不看稿，梳理逻辑**（9 个必答问题）：
- [ ] 什么是 Agent？和普通 LLM 调用、纯 RAG 区别？
- [ ] Tool-call 完整流程；没有框架怎么裸写循环？终止条件怎么防死循环？
- [ ] ReAct vs Plan-and-Execute 思路、适用场景？
- [ ] 记忆分哪几层？短期、长期分别怎么存？坑点？
- [ ] RAG 全链路，每一步有什么工程问题？怎么缓解幻觉？
- [ ] 为什么要做全链路 traceId 日志？Agent 服务排查难在哪里？
- [ ] 我怎么处理超时、重试、限流？长任务超时问题怎么解决？
- [ ] LangGraph 本质是什么？为什么原型用 Python、主项目选 Java？

---

### 课时 49｜简历项目描述（1.5h）

**做什么**
- 按 STAR 或"背景-方案-结果"结构写项目描述
- 突出**差异化**：Java 服务稳定性思维 + 从零手写循环的理解深度
- 准备 2-3 个**可以深入聊的技术细节**（比如并行工具调用、自我纠错机制、Prompt Cache 成本优化）

---

### 课时 50｜总复盘（1.5h）

**做什么**
- 回顾 `qa-notes.md` 全部 50 个问题，**确认都能答**
- 回顾 `agent-notes.md` 的踩坑清单 → **这些就是面试时"我踩过什么坑"的素材**
- 确认每个课时的验收项都打勾了

---

## 📌 节奏建议

| 情况 | 建议 |
|---|---|
| **只有 1 小时** | 做一个课时，或做课时的**前半部分**（很多课时可以分两次） |
| **有 3-4 小时** | 连做 2-3 个课时（**优先把标 ⚠️ 的段落整块做**） |
| **状态不好** | 只做验收项复查、整理笔记、读文档 —— **不算浪费时间** |
| **卡住超 40 分钟** | 跳过去，记进 `agent-notes.md`，继续往下 |
| **连续两周没进度** | 检查是不是某一课卡太久 → 降级目标（比如 W4 的混合检索直接跳过） |

## 📌 可以砍掉的部分（时间紧张时）

| 内容 | 优先级 |
|---|---|
| W4 混合检索（课时 30） | 🟢 可砍 |
| W5 LangGraph 原型（课时 34） | 🟡 尽量保留（理解状态机有帮助） |
| W7 OpenAPI（课时 45） | 🟡 保留（README 展示需要） |
| **W2 手写循环** | 🔴 **绝对不可砍** |
| **W1 SSE 流式** | 🔴 **绝对不可砍**（面试高频） |
| **W6 可观测性** | 🔴 不可砍（差异化所在） |
