# Agent 学习问题清单

> **这是索引，不是资料。** 每条问题后面标注答案在 `agent-notes.md` 哪一节。
> 用法：**先看问题，能答上来就跳过；答不上来再翻答案。**
>
> 建立：2026-09-14

---

## 🔴 W2 之前必须能答（核心，写循环时全部要用）

| # | 问题 | 答案 |
|---|---|---|
| 1 | `maxRound` 具体设多少？是算出来的循环次数吗？ | §7 Q1 |
| 2 | 复杂任务工具调用很多，会不会永远解决不了？ | §7 Q2 |
| 3 | 「轮次」和「工具调用次数」是一回事吗？ | §7 Q2 |
| 4 | `arguments` 里是我传给模型的参数吗？还是模型生成的？ | §7 Q5-4 |
| 5 | `arguments` 为什么是字符串而不是 JSON 对象？ | §7 Q5-4 |
| 6 | 模型会调不存在的工具名吗？ | §7 Q8 / Q10 |
| 7 | 模型的参数不符合我的 schema 要求怎么办？ | §7 Q10 |
| 8 | schema 里的 `maximum` / `enum` 是强制的吗？ | §7 Q10 |
| 9 | 模型返回「content 和 toolCalls 都空」时怎么处理？直接结束吗？ | §7 Q8 |
| 10 | 怎么防止模型反复调同一个工具？ | §7 Q8 |
| 11 | 撞到 `maxRound` 时该给用户返回什么？ | §7 Q8 |
| 12 | 模型调工具时 `content` 是什么值？会 NPE 吗？ | §7 Q6 |
| 13 | `type: "object"` 是什么意思？要建实体类吗？ | §7 Q5-2 |
| 14 | `assistant` 是什么角色？`role` 一共几种？ | §7 Q5-3 |
| 15 | 工具名 `get_current_time` 是固定写法吗？ | §7 Q5-1 |
| 16 | 每次请求都要把所有 tool 传给模型吗？开销浪费吗？ | §7 Q9 |
| 17 | 工具越多越好吗？真正的瓶颈是什么？ | §7 Q9 |

---

## 🟡 概念理解（W1/W3 会用到）

| # | 问题 | 答案 |
|---|---|---|
| 18 | 什么叫「prompt 诱导」？Thought 是模型天生就有的吗？ | §7 Q3 |
| 19 | Thought 一定反映模型真实的推理过程吗？ | §7 Q3 |
| 20 | 「Agent = LLM + 状态保存 + 循环」这三部分各负责什么？ | §6 |
| 21 | 循环是谁在转？模型自己会循环吗？ | §6 |
| 22 | 模型怎么知道上一轮发生了什么？ | §6 |
| 23 | 为什么要有 `maxRound`？ | §6 |
| 24 | Thought / Action / Observation 分别对应 API 里的哪个字段？ | §6 |
| 25 | Observation 是模型产生的还是我的代码产生的？ | §6 |
| 26 | ReAct 比「直接让模型答」好在哪？ | §6 |
| 27 | ReAct 和 Plan-and-Execute 的区别？ | 待学（W5） |
| 28 | 为什么多步任务用 Plan-and-Execute 比 ReAct 好？ | 待学（9/15 读） |

---

## 🟢 SSE 与工程（W1 实现要用）

| # | 问题 | 答案 |
|---|---|---|
| 29 | SSE 是什么？解决什么问题？ | §7 Q11 |
| 30 | SSE 和 WebSocket 的区别？ | §7 Q11 |
| 31 | Agent 服务为什么优先选 SSE？ | §7 Q11 |
| 32 | 流式返回里内容字段叫什么？（`message` 还是 `delta`？） | §2 / §7 Q11 |
| 33 | 流式怎么判断结束？（`finish_reason` 还是别的？） | §2 / §7 Q11 |
| 34 | SSE 最后一条 chunk 的 `delta` 是什么？要判空吗？ | §2 / §7 Q11 |
| 35 | W1 日志该记哪些字段？为什么？ | §3 |
| 36 | 请求的 model 和返回的 model 为什么不一样？日志记哪个？ | §2 |
| 37 | `finish_reason` 有哪几种取值？各自怎么处理？ | §2 |
| 38 | Prompt Cache 字段有什么用？怎么验证生效？ | §2 / §7 Q9 |
| 39 | `system_fingerprint` 是干什么的？ | §2 |

---

## ⚪ 环境与工具（遇到问题时查）

| # | 问题 | 答案 |
|---|---|---|
| 40 | PowerShell 里 `curl` 为什么用不了？ | §4.6 |
| 41 | curl 传 JSON 有空格就报 `Could not resolve host` 怎么解？ | §4.6 / §8 |
| 42 | Docker 里的 `127.0.0.1` 指的是谁？代理该怎么配？ | §4.4 |
| 43 | `.ps1` / `.cmd` 里的中文为什么会乱码？ | §4.7 |
| 44 | `git push` 报 `schannel: failed to receive handshake` 怎么查？ | §4.8 |
| 45 | `GH007: push would publish a private email address` 怎么解？ | §4.9 |
| 46 | key 应该放哪个文件？`.example` 和普通文件有什么区别？ | §4.10 |
| 47 | 提交前怎么做安全检查？ | §4.10 |
| 48 | 镜像源大面积失效，怎么判断某个源还活着？ | §4.5 |
| 49 | 排查问题的通用思路有哪几条？ | §5 |
| 50 | `wsl --install` 卡住能不能关窗口？ | §4.1 |
| 51 | 组件装完报「未启用虚拟化」，要先查 BIOS 吗？ | §4.2 |

---

## 📚 待办：还没学的问题

| 问题 | 什么时候学 |
|---|---|
| ReAct 和 Plan-and-Execute 的区别 | 9/15 读阿里云那篇 |
| 为什么多步任务 Plan-and-Execute 更好 | 9/15 |
| Function Calling 官方规范（中文文档） | 9/15 |
| LangGraph 是什么、为什么本质是状态机 | W5 |

---

## 📌 复习时机

| 时机 | 看哪些 |
|---|---|
| **W1 开始前**（9/18） | 🟢 组（29–39）+ ⚪ 组扫一眼 |
| **W2 开始前**（9/25） | 🔴 组（1–17）**全部** —— 这是 W2 的全部内容 |
| **W3 开始前**（10/2） | 🟡 组（18–19，prompt 诱导） |
| **W8 准备面试** | 全部过一遍，尤其 🔴 + 🟢 |

**面试直接能用的三个点**：
1. 工具数量与模型准确率的权衡（Q9）
2. Schema 只是提示、必须代码校验 + 错误回灌自我纠错（Q10）
3. 空响应/重复调用的降级设计，不假装成功（Q8）
