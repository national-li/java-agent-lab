# Agent 学习问答集（Q&A）

> 用途：**复习用**。这里记录学习过程中真正问出来的疑惑，以及当时的解答。
> 比抄概念术语有价值——**因为这是"卡住过的地方"，复习时优先级最高**。
>
> 来源：Day 1–2 学习过程中的实际提问
> 建立时间：2026-09-14

---

## Q1. `maxRound` 具体设多少？是算出来的吗？

### 结论

**算不出来，是"经验值 + 成本权衡"。W2 直接设 5。**

### 为什么是 5

路线图原文：
```
max_round = 5  // 最大工具调用轮次，防止死循环！这个限制是你的代码控制！
```

它不是"计算出循环次数"，而是**刹车**：

```
❌ 错误理解：我预计要转 5 圈
✅ 正确理解：最多让你转 5 圈，再转就掐了
```

**类比 `while` 循环的安全计数器** —— 不是预测次数，是设上限防失控。

### 权衡因素

| 考虑因素 | 倾向 |
|---|---|
| 典型任务需要几轮工具调用 | 大多数 **2-3 轮**够（查时间 → 算数 → 回答） |
| 留余量给稍复杂任务 | 4-5 轮 |
| **成本**（每轮都是一次 API 调用，都要花钱） | 不能太大 |
| **延迟**（每轮几秒网络往返，用户等不了） | 不能太大 |
| **防死循环**（模型可能反复调同一工具不收敛） | 必须有上限 |

**大多数情况下循环会在 `maxRound` 之前自然结束**（`finish_reason` 变 `stop`）。`maxRound` 只是**兜底的保险丝**。

### 什么时候该调整

| 场景 | 调整方向 |
|---|---|
| 简单任务（就是聊天） | 设小，1-2 |
| 复杂任务（多步搜索、文档分析） | 设大，8-10 |
| 生产环境 | **按成本预算倒推**：每轮 0.01 元 × 上限 = 可接受成本 |
| **面试标准答法** | "**按任务复杂度和成本预算权衡，不是固定值；同时必须有上限防死循环**" |

### 配套保护（W2 要一起做）

- 达到 `maxRound` 后返回提示，**日志记录"第 N 轮被强制终止"** → 才能发现是不是设小了
- **检测重复调用**：相同工具 + 相同参数反复调用 → 提前中断（比等 `maxRound` 更好）

---

## Q2. 复杂任务（工具调用很多）会不会永远解决不了？

### ⚠️ 先纠正前提：「轮」≠「工具调用次数」

**这是理解这个问题的关键。**

```
一轮循环 = 一次 LLM 调用
一轮里可以包含【多个】工具调用（并行工具调用）
```

现代模型支持 **parallel tool calls**，模型可以在**一次响应里**返回多个工具调用：

```json
// 模型在【一轮】里返回了 3 个工具调用
"tool_calls": [
  { "id": "call_1", "function": { "name": "get_weather", "arguments": "{\"city\":\"北京\"}" } },
  { "id": "call_2", "function": { "name": "get_weather", "arguments": "{\"city\":\"上海\"}" } },
  { "id": "call_3", "function": { "name": "get_weather", "arguments": "{\"city\":\"广州\"}" } }
]
```

**你的代码把这 3 个都执行完，结果一起塞回 messages，然后才进第 2 轮。**

### 重新算数量级

| 场景 | 工具调用次数 | **实际轮次** |
|---|---|---|
| 查 1 个城市天气 | 1 | 1 |
| **查 5 个城市天气** | **5** | **1**（并行） |
| 查天气 → 推荐穿衣 → 查航班 | 3 | 3 |
| 复杂文档分析（多次检索） | 10+ | 3-5（每轮并行几个） |

**「2-3 轮」指 2-3 次 LLM 往返，不是 2-3 个工具调用 —— 两者差一个数量级。**

### 所以 maxRound=5 并不限制正常任务

```
正常路径：finish_reason 变 "stop" → 自然结束（通常 2-4 轮）
异常路径：模型卡住反复调工具 → maxRound 掐断
```

**正常的 3 轮任务根本碰不到上限。**

### 复杂任务的四层手段

**① 提高 maxRound**
按实际分布定，**取 p95 再加余量**，别拍脑袋。
> 💡 这正是 W6 可观测性的价值 —— 日志记了每轮调用次数，你才知道该设多少。

**② 让工具"一次给更多"（工程上最有效）**
```
❌ get_weather(city)            → 查 5 城要 5 次
✅ get_weather_batch(cities[])   → 1 次搞定
```
**工具设计的粒度直接决定轮次数量**，这是 Agent 工程的核心技巧之一。

**③ 用 Plan-and-Execute 替代 ReAct（W5 内容）**
ReAct 是"走一步看一步"，**步数多了容易失控**；Plan-and-Execute 先出完整计划再执行，**天然适合多步任务**。
> 这就是为什么路线图 W5 要教它 —— 它是 ReAct 处理长任务的补充方案。

**④ 拆分任务（上层设计）**
把"分析 100 篇文档"拆成 N 个子任务，每个独立跑一个 Agent。

### ⚠️ 但"有些问题确实做不到"是对的，要诚实处理

| 情况 | 正确处理 |
|---|---|
| 撞到 maxRound | **不能只返回"我无法继续完成请求"** —— 要告知**已完成的部分**和**卡在哪一步** |
| 模型反复调同一工具 | **检测循环**（相同工具+参数重复 → 提前中断） |
| 任务本质超出能力 | 明确说明"需 N 步，超出单次请求限制，建议拆分" |

> **好的设计不是假装能做完，而是优雅地告诉用户做不到以及为什么。** 面试加分。

---

## Q3. 什么叫"prompt 诱导"？什么叫"天生就有"？

### ⭐ 这是最容易被误解、也最能体现是否真懂的点

### 先明确"天生就有"指的是什么错误认知

> ❌ **错误认知**：模型内部真的在"思考"，像人一样先想清楚再回答，`Thought` 是它**内在思维过程的自然流露**。

**如果这是真的**，你什么都不用做，模型自己就会输出"让我先想想……"。**但事实不是。**

### 模型的真实运作方式

```
输入文字 → 预测下一个词 → 输出文字
          ↑ 这里面没有"思考"这一步
```

**模型是个文字接龙机器**，只做一件事：根据已有文字，预测下一个最可能出现的词。

### "诱导"就是这个意思

**`Thought` 是你要求它写，它才写的。你不要求，它就不写。**

**验证方法（W3 你会亲手做）**：

| 你传的 prompt | 模型输出 |
|---|---|
| `[{"role":"user","content":"现在几点"}]` | `"我不知道当前时间。"` ← **没有 Thought** |
| `[{"role":"system","content":"请先写出你的思考过程，再决定是否调用工具"}, {"role":"user","content":"现在几点"}]` | `"我需要知道当前时间，应该调用 get_current_time。"` ← **有 Thought 了** |

**同一句话、同一个模型，只多了一句 system prompt，Thought 就出现了。**

**这就是"诱导"** —— 不是唤醒了它内在的思考，而是**用 prompt 把这种输出格式"钓"出来了**。

### 为什么 Thought 有用（虽然是"钓"出来的）

```
Thought 不是思维的展示，而是思维的工具。
```

它写出来后会成为**后续输出的"上文"**，相当于打了个草稿 —— **所以它确实改善了结果**。类似你做题时在草稿纸上写步骤。

### ⚠️ 更反直觉的一点：Thought 可能是编的

因为 `Thought` 也是**生成出来的文字**，它**不一定反映模型真实的计算过程**。

```
模型输出 Thought: "我需要先查天气，再决定穿什么"
实际它做的：     直接根据训练数据猜了个回答
```

**这个 Thought 是"事后编的合理化解释"** —— 看起来像推理，其实是生成出来的、看起来像推理的文字。

> 学术上叫 **"unfaithful chain-of-thought"（不忠实的思想链）**
> **面试时提到这点是加分项** —— 说明你知道 CoT 的局限，不是盲信。

### 认知对照表（自测）

| 认知 | 对错 |
|---|---|
| 模型内部有个"思考"步骤，Thought 是它的流露 | ❌ |
| Thought 是 prompt 要求它输出的**文字格式** | ✅ |
| 去掉 prompt 里的要求，Thought 就消失 | ✅ |
| Thought 写出来能改善结果（相当于草稿） | ✅ |
| Thought 一定反映真实推理过程 | ❌ **可能完全是编的** |

> **一句话总结**：
> **模型的"思考"是说出来的，而不是"说出来的东西反映了思考"。**
> **顺序是反的 —— 是"说出来"这个动作本身，帮它把结果算对了。**

### 与 W3 的关系

W3 用 Spring AI 时，**thought 是"配置出来的"**：你在 ChatClient 上开启某个选项，框架帮你在 prompt 里加上诱导语句，你才看得到 thought。
**它不是模型自带的开关。**

---

## Q4. W1 / W2 / W3 是什么？

**W = Week（周）**，来自路线图 `深渊票.md`。

| 缩写 | 时间 | 干什么 |
|---|---|---|
| **预备周** | 9/13 – 9/17 | 环境 + 读综述（**当前阶段**） |
| **W1** | 9/18 – 9/24 | **SpringBoot 写 LLM 客户端**：超时、重试、SSE 流式、Redis 会话 |
| **W2** | 9/25 – 10/1 | ⭐ **纯 Java 手写 Tool Calling 循环**（全路线最难最重要） |
| **W3** | 10/2 – 10/8 | **Spring AI 重构** + 实现 **ReAct**（thought 输出）+ 联网搜索工具 |
| **W4** | 10/9 – 10/15 | **记忆系统 + RAG**（PgVector 向量检索） |
| **W5** | 10/16 – 10/22 | **Plan-and-Execute** + Python LangGraph 原型 |
| **W6** | 10/23 – 10/29 | **工程化 I**：异常、限流、鉴权、traceId 可观测性 |
| **W7** | 10/30 – 11/5 | **工程化 II**：持久化、长任务方案、docker compose |
| **W8** | 11/6 – 11/12 | **文档 + 简历**（不写新代码） |

### 为什么"懂 ReAct 才能进 W1"？

**W1 是"还没开始做 Agent"的阶段** —— 只做最基础的 LLM 调用服务。
路线图原文：**"这周绝对不要碰 Tool Calling、不要碰 RAG"**。

那为什么还要先懂 ReAct？

| 原因 | 说明 |
|---|---|
| **地基意识** | W1 的 `LlmClient` 是 W2 循环的基础组件，要知道它最终服务什么 |
| **避免走弯路** | 知道"模型只能建议、执行靠我"，就不会在 W1 里试图让模型自己做事 |
| **SSE 的用途** | W1 的流式输出，正是为了让 **W2 的 Thought 过程实时显示给用户** |
| **面试连贯性** | 简历写"实现了 Agent"，得能说清整体设计 |

**打比方**：
```
W1 = 造发动机（还不知道装什么车上）
W2 = 造完整动力系统 + 手写控制系统   ← 核心
W3 = 换成成熟的电控系统（Spring AI）
```

**答不上来也不是不能进 W1，但 W2 会立刻撞墙** —— 因为 W2 的全部内容就是这个循环。

---

## 📌 复习优先级建议

**按"卡住过的程度"排序，优先复习**：

| 优先级 | 内容 | 为什么 |
|---|---|---|
| 🥇 | **Q3 prompt 诱导** | 最容易误解；W3 会直接用到（配置 thought） |
| 🥇 | **Q2 轮次 vs 工具调用次数** | 概念性错误，会一直影响理解 |
| 🥈 | **Q1 maxRound 定位** | W2 立刻要用；面试高频 |
| 🥉 | Q4 W1-W8 对照 | 查表用，不用背 |

**复习时机建议**：
- **W1 开始前**：过一遍 Q4（知道自己要去哪）
- **W2 开始前**：重点看 Q1 + Q2（马上要用）
- **W3 开始前**：重点看 Q3（thought 配置）
- **W8 准备面试**：全部过一遍，这些就是面试问答的素材
